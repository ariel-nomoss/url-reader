{
  "name": "vendors-performance",
  "version": "1.0.0",
  "description": "Analysis of web pages performance from the vendors being tested to replace WordPress.",
  "main": "src/index.ts",
  "author": "Ariel Rahmane",
  "license": "MIT",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon",
    "build": "rimraf ./build && tsc",
    "start": "yarn build && node build/index.js"
  },
  "devDependencies": {
    "@types/node": "^16.9.2",
    "@types/node-fetch": "^2",
    "node-fetch": "^2.6.1",
    "nodemon": "^2.0.12",
    "rimraf": "^3.0.2",
    "ts-node": "^10.2.1",
    "typescript": "^4.4.3"
  },
  "private": true,
  "dependencies": {
    "dotenv": "^10.0.0",
    "lighthouse-batch": "^8.1.1"
  }
}
